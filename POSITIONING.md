# Positioning

Half-Life is verified state compaction for long-running AI or software-agent jobs.

It is useful when a system needs to carry forward a smaller, auditable state package without silently changing the decisions that the complete signed history would produce.
