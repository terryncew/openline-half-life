# Demo

Run:

```bash
openline-half-life demo --out build/demo
```

The example compacts the complete 70-turn signed job history, verifies the compact state against an independent replay of the raw history, and verifies recovery of every archived source receipt.

The example is deterministic and uses public demonstration keys. Its compression ratio is not a universal benchmark.
